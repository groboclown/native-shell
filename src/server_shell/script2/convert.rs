//! Convert the script 2 format into the AST format.

pub fn convert_to_ast(script: &model::Script2) -> Result<model::Ast, String> {
    let mut ast = model::Ast::new();
    
    Err("Conversion from Script2 to AST is not implemented yet.".to_string())
}
